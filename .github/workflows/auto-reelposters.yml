name: Auto Reelposters Generator

on:
  schedule:
    # Elke zondag 08:30 CET (07:30 UTC) — na de slides run (07:00)
    - cron: '30 7 * * 0'

  workflow_dispatch:
    inputs:
      max_posters:
        description: 'Max aantal posters om te genereren (standaard: alle pending)'
        required: false
        type: string

permissions:
  contents: write

jobs:
  generate-reelposters:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium dependencies
        run: |
          sudo apt-get update -q
          sudo apt-get install -y -q \
            libgbm-dev \
            libnss3 \
            libatk-bridge2.0-0 \
            libdrm2 \
            libxkbcommon0 \
            libxcomposite1 \
            libxdamage1 \
            libxfixes3 \
            libxrandr2 \
            libglib2.0-0 \
            libpango-1.0-0 \
            libcairo2 \
            libasound2t64 || true

      - name: Check for pending topics
        id: check_queue
        run: |
          PENDING=$(node -e "
            const d = JSON.parse(require('fs').readFileSync('reelposter-topics.json','utf8'));
            const p = d.queue.filter(t => t.status === 'pending');
            console.log(p.length);
          ")
          echo "pending_count=$PENDING" >> $GITHUB_OUTPUT
          echo "Pending topics: $PENDING"

      - name: Generate reelposters
        if: steps.check_queue.outputs.pending_count != '0'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node scripts/generate-reelposters.js

      - name: Check if files changed
        id: check_changes
        run: |
          if git diff --quiet && git diff --staged --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push generated posters
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "ZenBTW Bot"
          git add reelposters/ reelposter-topics.json
          COUNT=$(jq '.sets | length' reelposters/manifest.json)
          git commit -m "feat: auto-generate reelposters (${COUNT} total)"
          git pull --rebase origin main
          git push

      - name: Summary
        run: |
          PENDING=$(jq '.queue | map(select(.status=="pending")) | length' reelposter-topics.json)
          TOTAL=$(jq '.published | length' reelposter-topics.json)
          echo "### Reelposters Generation" >> $GITHUB_STEP_SUMMARY
          echo "- **Gegenereerd deze run:** ${{ steps.check_queue.outputs.pending_count }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Totaal gepubliceerd:** $TOTAL posters" >> $GITHUB_STEP_SUMMARY
          echo "- **Topics resterend:** $PENDING" >> $GITHUB_STEP_SUMMARY
          echo "- **Bekijk in admin:** https://zenbtw.nl/admin → Reelposters" >> $GITHUB_STEP_SUMMARY
